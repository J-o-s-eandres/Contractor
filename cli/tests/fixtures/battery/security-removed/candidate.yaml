components:
  securitySchemes:
    oauth2:
      flows:
        implicit:
          authorizationUrl: https://example.com/auth
          scopes:
            read: read access
      type: oauth2
info:
  title: T
  version: '1'
openapi: 3.0.0
paths:
  /health:
    get:
      responses:
        '200':
          description: ok
